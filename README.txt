A simple local in-home Ollama setup.  My installation is using nssm on Windows to make it a service.  I prefer
configuration files by Linux.  Run the config.sh in WSL2 and be sure to install ollama a strictly a client
on the WSL2 side.

Note my development machine is Omarchy.  I just happened to have a Windows machine with a nice GPU handy.


